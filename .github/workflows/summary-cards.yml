name: Generate profile summary cards

on:
  schedule:
    - cron: '27 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate summary cards
        uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          USERNAME: prsdx
          UTC_OFFSET: 5.5

      - name: Keep only github_dark + default themes
        run: find profile-summary-card-output -mindepth 1 -maxdepth 1 -type d ! -name github_dark ! -name default -exec rm -rf {} +

      - name: Commit cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile-summary-card-output/
          git diff --cached --quiet || git commit -m "Update profile summary cards [skip ci]"
          git push